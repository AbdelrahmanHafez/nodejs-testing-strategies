const assert = require('assert');

describe('A feature', () => {
  describe('A scenario', () => {
    it('Adds stuff', async () => {
      const a = 'hello';
      assert.equal(5 + 5, 10);
    });
  });
});